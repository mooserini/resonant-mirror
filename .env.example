# This portfolio build does not call the Gemini API or require a server API key.
# Google Chat uses the browser Firebase configuration and the signed-in user's OAuth token.
# Do not put server credentials into VITE_* variables or frontend source.
